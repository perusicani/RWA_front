import { getByTitle } from '@testing-library/react';
import React from 'react';


function Tasks() {


    // Will need:
    //     table for all Tasks
    //     has -> Title, description, action buttons -> update (check off tasks)

    return (
        <div>Tasks</div>

    );

}

export default Tasks;